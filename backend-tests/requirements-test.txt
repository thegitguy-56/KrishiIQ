# Test runner
pytest==8.3.3
pytest-json-report==1.5.0
pytest-xdist==3.6.1

# HTTP client used against the live server
httpx==0.27.2

# JWT crafting for DAST tamper tests (independent of the app's own 'jose' lib)
PyJWT==2.9.0

# Report generation
openpyxl==3.1.5

# SAST (also invoked directly in the workflow, listed here for local parity)
bandit==1.7.10
pip-audit==2.7.3
